node_modules
dist
dist-ssr
.vite
.DS_Store
*.local
.env
.env.local
.env.*.local
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / tooling config
.claude/
.vscode/
.idea/

# Raw source media (kept locally as reference; web-optimised copies live
# under public/). Excluded because GitHub limits files to 100 MB.
*.MOV
*.mov
